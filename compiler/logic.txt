
if cleaning
	delete local folder
	
per file
	if cleaning
		delete shared
		delete local
	if local exists and older than source
		compile = true
	else if local not exists
		compile = true
	if compile
		compile, save to shared
	copy shared to local
	