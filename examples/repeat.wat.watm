
module
	
	import "../library/utility.watm"
	import "../library/memory.watm"
	import "../library/string.watm"
	import "../library/types.watm"
	import "host" "table" (table 1 anyfunc)
	memory (import "host" "memory") 1
	
	func main
		
		memory_bootstrap
		
		repeat with i from 100 to 300 every 100
			print_integer i
			repeat with j from 10 to 30 every 10
				print_integer j
				repeat with k from 1 to 3 every 1
					print_integer k
					