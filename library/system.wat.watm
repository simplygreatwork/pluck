(module
	
	(import "./utility.watm")
	(import "./memory.watm")
	(memory (import "host" "memory") 1)
	(import "host" "table" (table 1 anyfunc))
	
	(func $system_bootstrap
		
		(call $memory_bootstrap)
	)
	
	(func $system_prototypes_create
		
		
	)
)
